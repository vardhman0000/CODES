// WAP that makes user guess a random number, if guess is higher than random num display "too high"
// if lower guess then display "too low"
#include<stdio.h>
#include<stdlib.h>
int main(){
    printf("%d", rand()) ;
}
