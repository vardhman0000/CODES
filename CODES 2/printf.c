#include<stdio.h>
void main(){
    // int a ;
    // a=printf("Vardhman Jain");
    // printf("\n%d", a);

    if(!printf(""))
        printf("Okkk");
    else
        printf("Hii");
}

// printf() function also returns the length of argument passed into it
// variable is location where you can store any value
// ampersand (&) signifies address of a variable
// in pre incre/decre first calculation is ddone then assignment happens and cycle runs once ony
// on post incre/decre assignment happens first and then current cycle ends and then again this happens for second time and so on....

// a = 1,2,3 ---> Execution right to left ---> '1' will be assigned to a
// b = (1,2,3) ---> Execution left to right ---> '3' will be assigned to a
